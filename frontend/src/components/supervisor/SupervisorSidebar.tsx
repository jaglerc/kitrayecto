import { useNavigate } from "react-router";
interface SupervisorSidebarProps { name: string; active?: "inicio"|"usuarios"|"vehiculos"|"documentos"|"roles"|"notificaciones"; }
const items=[{id:"inicio",icon:"⌂",label:"Inicio"},{id:"usuarios",icon:"♧",label:"Usuarios"},{id:"vehiculos",icon:"🚚",label:"Vehículos"},{id:"documentos",icon:"▧",label:"Documentos"},{id:"roles",icon:"◇",label:"Roles y permisos"},{id:"notificaciones",icon:"♢",label:"Notificaciones"}]as const;
export default function SupervisorSidebar({name,active="inicio"}:SupervisorSidebarProps){const navigate=useNavigate();const logout=()=>{localStorage.removeItem("token");localStorage.removeItem("user");navigate("/login",{replace:true});};return <aside className="fixed inset-y-0 left-0 z-40 hidden w-64 flex-col border-r border-gray-200 bg-white px-4 py-6 md:flex"><img src="/ruta-aviario.png" alt="El Aviario" className="h-20 w-48 object-contain"/><div className="mt-2 rounded-xl border border-gray-100 p-3"><p className="text-sm font-semibold text-gray-900">Hola, {name} 👋</p><p className="text-xs text-gray-500">Supervisor</p></div><nav className="mt-5 space-y-2">{items.map(item=><button key={item.id} type="button" disabled={item.id!=="inicio"} title={item.id!=="inicio"?"Se desarrollará en el siguiente módulo":undefined} className={`flex w-full items-center gap-3 rounded-xl px-4 py-3 text-sm transition ${item.id===active?"border border-amber-400 bg-amber-50 font-semibold text-gray-900":"text-gray-500 hover:bg-gray-50 disabled:cursor-not-allowed disabled:opacity-60"}`}><span aria-hidden="true" className="w-5 text-center text-lg">{item.icon}</span>{item.label}</button>)}</nav><button type="button" onClick={logout} className="mt-auto flex items-center gap-3 border-t border-gray-100 px-4 pt-5 text-sm text-gray-600"><span className="text-lg">↪</span>Cerrar sesión</button></aside>}
