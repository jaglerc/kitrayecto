import type { ConsultationCategory } from "../../services/consultation.service";
interface ConsultationCategoryCardProps { category: ConsultationCategory; title: string; description: string; icon: string; onClick: () => void; }
export default function ConsultationCategoryCard({ title, description, icon, onClick }: ConsultationCategoryCardProps) { return <button type="button" onClick={onClick} className="flex min-h-36 flex-col items-center justify-center rounded-2xl border border-gray-100 bg-white p-4 text-center shadow-sm transition active:scale-95"><span className="flex h-14 w-14 items-center justify-center rounded-full bg-gray-50"><img src={icon} alt="" className="h-8 w-8 object-contain" /></span><strong className="mt-3 text-sm text-gray-900">{title}</strong><span className="mt-1 text-[10px] leading-relaxed text-gray-500">{description}</span><span className="mt-2 text-amber-500">→</span></button>; }
