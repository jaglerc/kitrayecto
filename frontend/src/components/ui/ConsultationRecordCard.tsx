import type { ConsultationItem } from "../../services/consultation.service";
interface ConsultationRecordCardProps { item: ConsultationItem; icon: string; onClick: () => void; }
export default function ConsultationRecordCard({ item, icon, onClick }: ConsultationRecordCardProps) { const time=new Intl.DateTimeFormat("es-CO",{hour:"2-digit",minute:"2-digit"}).format(new Date(item.occurredAt)); return <button type="button" onClick={onClick} className="grid w-full grid-cols-[42px_45px_1fr_18px] items-center gap-2 rounded-xl border border-gray-200 bg-white p-3 text-left shadow-sm"><span className="flex h-10 w-10 items-center justify-center rounded-full bg-gray-50"><img src={icon} alt="" className="h-6 w-6 object-contain" /></span><span className="text-xs font-semibold text-gray-700">{time}</span><span className="min-w-0"><span className="block truncate text-xs font-bold text-gray-900">{item.title} · {item.plate}</span><span className="mt-1 block truncate text-[10px] text-gray-500">{item.subtitle}</span><span className="mt-1 inline-block rounded-full bg-emerald-50 px-2 py-0.5 text-[9px] font-medium text-emerald-600">{item.status}</span></span><span className="text-xl text-gray-500">›</span></button>; }
